# 🦁 Юнит-тестирование классов Lion, Cat и Feline (Maven + Mockito + JUnit)

Проект содержит юнит-тесты для классов **Lion**, **Cat** и **Feline**.  
В проекте реализована инъекция зависимостей, моки Mockito, параметризованные тесты и покрытие кода JaCoCo.

---

## 📌 Функциональность, покрытая тестами

### **1. Класс Feline**
Проверяется:
- корректный расчёт количества котят;
- работа метода получения типа животного;
- вычисление рациона хищников.

Тесты полностью покрывают поведение класса.

---

### **2. Класс Cat**
Проверяется:
- возвращаемый звук животного;
- корректность работы методов с учётом делегирования в `Feline`.

---

### **3. Класс Lion**
Проверяется:
- инъекция зависимости `Feline`;
- корректное определение пола (`самец` / `самка`);
- корректная обработка некорректного значения пола;
- расчёт количества котят;
- получение рациона, делегированного в `Feline`.

Используются **моки Mockito** для изоляции поведения.

---

## 🧩 Инъекция зависимостей
Класс `Lion` не зависит напрямую от `Feline`:  
зависимость передаётся через **конструктор**, что позволяет мокать её в тестах.

---

## 🔁 Параметризация
Применена для:
- проверки валидности пола льва;
- сценариев расчёта котят для разных входных данных.

---

## 🦁 Дополнительное задание — Лев Алекс
Создан наследник `Lion` — **Alex**, персонаж из «Мадагаскара».

Проверяется:
- метод `getFriends()` — список друзей (Марти, Глория, Мелман);
- метод `getPlaceOfLiving()` — Нью-Йоркский зоопарк;
- переопределённый `getKittens()` — у Алекса их нет;
- собственный конструктор (Алекс всегда самец);
- корректная работа наследования и делегирования.

---

## 🧰 Используемые технологии
- **Java + Maven**
- **JUnit 4**
- **Mockito**
- **JaCoCo** (покрытие ≥ 100% для Lion, Cat, Feline)

---

## 📊 Покрытие JaCoCo
Проект настроен так, чтобы классы:
- **Feline**
- **Cat**
- **Lion**

были покрыты **на 100%**.

---

## ▶️ Запуск тестов
```bash
mvn clean test
